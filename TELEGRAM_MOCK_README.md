# Telegram Mock для локальной разработки

## Что это?

Mock (эмуляция) Telegram WebApp API для локального тестирования приложения без необходимости запускать его через Telegram.

## Как работает?

1. **Автоматическое определение**: Мок автоматически загружается только если приложение НЕ запущено в Telegram
2. **Визуальный индикатор**: В правом верхнем углу отображается "🔧 Mock Mode"
3. **Полная эмуляция**: Все основные методы Telegram WebApp API эмулируются

## Что эмулируется?

### ✅ Основные методы
- `expand()` - раскрытие приложения
- `ready()` - готовность приложения
- `setHeaderColor()` - цвет хедера
- `close()` - закрытие приложения

### ✅ BackButton
- `show()` / `hide()` - показать/скрыть кнопку назад
- `onClick()` - обработчик клика
- Визуальная кнопка в левом верхнем углу

### ✅ MainButton
- `show()` / `hide()` - показать/скрыть главную кнопку
- `setText()` - изменить текст
- `enable()` / `disable()` - активировать/деактивировать
- Визуальная кнопка внизу экрана

### ✅ Алерты и попапы
- `showAlert()` - показать алерт
- `showConfirm()` - показать подтверждение
- `showPopup()` - показать попап

### ✅ Данные пользователя
- `initData` - строка с данными инициализации
- `initDataUnsafe` - объект с распарсенными данными
- Mock пользователь по умолчанию:
  ```javascript
  {
    id: 123456789,
    first_name: 'Test',
    last_name: 'User',
    username: 'testuser',
    language_code: 'ru'
  }
  ```

## Как использовать?

### Базовое использование - С моком (по умолчанию)

Просто запустите приложение локально:

```bash
python run.py
```

Мок будет активен по умолчанию. Откройте `http://localhost:5000` в браузере.

### Отключить мок (для тестирования через ngrok)

**Вариант 1: Через переменную окружения**
```bash
TELEGRAM_MOCK=false python run.py
```

**Вариант 2: Создать/изменить файл `.env`**
```bash
# В файле .env
TELEGRAM_MOCK=false
```

Затем просто:
```bash
python run.py
```

**Вариант 3: В config.py (для постоянного изменения)**
```python
TELEGRAM_MOCK = False  # Всегда выключен
```

### Смена тестового пользователя

В консоли браузера (F12):

```javascript
// Сменить пользователя
window.TelegramMock.setUser({
    id: 987654321,
    first_name: 'Другой',
    last_name: 'Пользователь',
    username: 'anotheruser'
});

// Посмотреть текущего пользователя
console.log(window.TelegramMock.currentUser);

// Получить initData
console.log(window.TelegramMock.getInitData());

// Перезагрузить аутентификацию
window.TelegramMock.reloadAuth();
```

### Тестирование BackButton

BackButton автоматически показывается на страницах проектов. Кликните по визуальной кнопке "← Back" в левом верхнем углу.

### Тестирование MainButton

Если ваш код использует MainButton:

```javascript
const tg = window.Telegram.WebApp;
tg.MainButton.setText('Сохранить');
tg.MainButton.onClick(() => {
    console.log('Button clicked!');
});
tg.MainButton.show();
```

Вы увидите кнопку внизу экрана.

## Отличия от реального Telegram

### ⚠️ Важно знать

1. **Безопасность Mock**: Mock использует специальный хэш `mock_hash_for_local_dev`, который автоматически распознается сервером в dev режиме. **В production это нужно отключить!**
2. **Хэптики**: `HapticFeedback` только логирует в консоль, не создает вибрацию
3. **Ссылки**: `openTelegramLink()` показывает alert вместо открытия Telegram
4. **sendData()**: Только логирование, данные не отправляются боту

**🔒 Production безопасность**: В production режиме обязательно установите `TELEGRAM_MOCK=false` и настройте правильный `TELEGRAM_BOT_TOKEN`. Mock хэш не будет приниматься при наличии настоящего бот токена.

### ✅ Что работает так же

- Аутентификация пользователей (с mock данными)
- Все UI элементы (кнопки, алерты)
- Навигация и роутинг
- Все ваши API вызовы

## Отключение мока

### Временно (одна сессия)
```bash
TELEGRAM_MOCK=false python run.py
```

### Постоянно через .env
Создайте/измените файл `.env`:
```bash
TELEGRAM_MOCK=false
```

### Постоянно через config.py
```python
# В config.py
TELEGRAM_MOCK = False
```

## Режимы работы приложения

### 1. Локально с моком (разработка) - ПО УМОЛЧАНИЮ
```bash
# Либо просто:
python run.py

# Либо явно:
TELEGRAM_MOCK=true python run.py
```
- Адрес: `http://localhost:5000`
- Мок активен ✅
- Индикатор "🔧 Mock Mode"
- Работает без интернета

### 2. Через ngrok (тестирование в Telegram)
```bash
TELEGRAM_MOCK=false python run.py
```
- Адрес: `https://xxx.ngrok.io`
- Мок отключен
- Используется настоящий Telegram WebApp API
- Нужен интернет

### 3. Production в Telegram
```bash
# В .env или переменных окружения
TELEGRAM_MOCK=false
```
- Мок отключен
- Полная функциональность Telegram

## Консольные команды для отладки

```javascript
// Проверить, активен ли мок
console.log(window.TelegramMock ? 'Mock активен' : 'Реальный Telegram');

// Посмотреть все доступные методы
console.dir(window.Telegram.WebApp);

// Симулировать разные user_id для тестирования доступа
window.TelegramMock.setUser({ id: 111 }); // Пользователь 1
window.TelegramMock.setUser({ id: 222 }); // Пользователь 2

// Очистить сессию и аутентификацию
sessionStorage.clear();
window.location.reload();
```

## FAQ

**Q: Почему мой код не работает с моком?**  
A: Откройте консоль (F12) и проверьте логи. Мок логирует все вызовы.

**Q: Как тестировать с разными пользователями?**  
A: Используйте `window.TelegramMock.setUser()` в консоли браузера.

**Q: Мок не загружается**  
A: Проверьте, что скрипт `telegram-mock.js` загружается ПЕРЕД `telegram-web-app.js` в HTML.

**Q: Как протестировать в реальном Telegram?**  
A: Используйте ngrok для создания публичного URL и настройте бот с вебхуком.

## Дополнительные возможности

### Кастомизация mock пользователя

Отредактируйте `telegram-mock.js`:

```javascript
const mockUser = {
    id: 123456789,
    first_name: 'Ваше Имя',
    last_name: 'Фамилия',
    username: 'yourusername',
    language_code: 'ru',
    is_premium: true, // Премиум подписка
    allows_write_to_pm: true
};
```

### Логирование всех вызовов

Мок автоматически логирует все вызовы методов в консоль с префиксом `[Mock]`.

---

**Готово!** Теперь вы можете разрабатывать и тестировать Telegram Mini App локально без интернета! 🎉
