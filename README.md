# check - Система управления проектами

## Описание

**check** — это веб-приложение для отслеживания задач и управления проектами с интегрированным Telegram ботом. 

### Основные возможности

- **Управление проектами** — на главной странице видно все проекты, чтобы никакой не остался без внимания
- **Трекинг задач** — задачи позволят отслеживать прогресс проектов
- **Быстрые заметки** — сохраняйте мимолетные мысли, чтобы потом разобрать
- **Итоги** — каждый вечер смотрите свой прогресс за день
- **Уведомления** — напомним о дедлайнах и важных вещах
- **Telegram Mini App** — доступ к проектам прямо из Telegram
- **Ежедневные напоминания** — бот напоминает посмотреть результаты каждый вечер

## Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/eledays/check.git
   cd check
   ```

2. **Создайте виртуальное окружение**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate  # Для Linux/macOS
   # или
   .venv\Scripts\activate  # Для Windows
   ```

3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте Telegram бота** (опционально, но рекомендуется)
   
   a. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте команду `/newbot`
   - Выберите имя и username для бота
   - Получите токен бота (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
   
   b. Настройте Mini App:
   - Отправьте `/newapp` в @BotFather
   - Выберите вашего бота
   - Введите название Mini App
   - Введите описание
   - Загрузите иконку (опционально)
   - Введите URL вашего приложения (для локальной разработки используйте ngrok)
   
   c. Создайте файл `.env` в корне проекта:
   ```bash
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   BOT_REMINDER_TIME=20:00
   BOT_TIMEZONE=Europe/Moscow
   BOT_REMINDERS_ENABLED=true
   ```

5. **Инициализируйте базу данных**
   ```bash
   flask --app run.py db upgrade
   ```

6. **Запустите приложение**
   ```bash
   python run.py
   ```
   
   💡 **Локальная разработка**: По умолчанию включен Telegram Mock для работы без интернета.  
   Подробнее: [TELEGRAM_MOCK_README.md](TELEGRAM_MOCK_README.md)
   
   🤖 **С ботом**: Если вы настроили `TELEGRAM_BOT_TOKEN`, бот запустится автоматически и будет отправлять напоминания в указанное время.

7. **Откройте в браузере**
   ```
   http://127.0.0.1:5000
   ```

---

## Технологии

| Технология | Назначение |
|-----------|-----------|
| **Flask** | Веб-фреймворк |
| **SQLAlchemy** | ORM для работы с базой данных |
| **Flask-Migrate** | Управление миграциями БД |
| **SQLite** | База данных |
| **Jinja2** | Шаблонизатор |
| **pyTelegramBotAPI** | Telegram Bot API |
| **Material Symbols** | Иконки |
| **Inter Font** | Типографика |

---

## Telegram Bot

### Возможности бота

- 🤖 **Команды бота**:
  - `/start` - Начать работу с ботом
  - `/app` - Открыть Mini App
  - `/summary` - Получить итоги дня
  - `/settings` - Посмотреть настройки уведомлений
  - `/remind` - Управление уведомлениями
  - `/help` - Справка по командам

- 📅 **Ежедневные напоминания**:
  - Бот автоматически отправляет напоминания в указанное время
  - Показывает статистику выполненных задач
  - Уведомляет о проектах, требующих внимания
  - Отображает количество незавершённых задач
  - **Персональные настройки**: каждый пользователь может установить своё время и часовой пояс
  - **Гибкое управление**: включайте и отключайте уведомления когда удобно

- 📱 **Mini App интеграция**:
  - Полноценное веб-приложение внутри Telegram
  - Кнопка быстрого доступа к приложению
  - Удобная работа с проектами прямо из Telegram

### Настройка бота

1. **Создайте бота через @BotFather**:
   ```
   /newbot
   Название: Check Project Manager
   Username: your_check_bot (должен быть уникальным)
   ```
   
2. **Настройте Mini App**:
   ```
   /newapp
   Выберите вашего бота
   Название: Check
   Описание: Управление проектами и задачами
   ```

3. **Получите токен** и добавьте в `.env`:
   ```bash
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

4. **Настройте параметры напоминаний** (опционально - по умолчанию):
   ```bash
   # Время отправки ежедневных напоминаний по умолчанию (формат 24ч)
   # Каждый пользователь может изменить это для себя через бота
   BOT_REMINDER_TIME=20:00
   
   # Часовой пояс для напоминаний по умолчанию
   # Каждый пользователь может изменить это для себя через бота
   BOT_TIMEZONE=Europe/Moscow
   
   # Включить/выключить систему напоминаний глобально
   BOT_REMINDERS_ENABLED=true
   ```
   
   **Важно:** Настройки BOT_REMINDER_TIME и BOT_TIMEZONE используются только как значения по умолчанию. Каждый пользователь может установить своё время и часовой пояс через команды бота `/remind time` и `/remind tz`.

5. **Запустите приложение** - бот запустится автоматически:
   ```bash
   python run.py
   ```

### Использование бота

1. Найдите своего бота в Telegram по username
2. Отправьте `/start` для начала работы
3. Нажмите кнопку "📱 Открыть приложение" для доступа к Mini App
4. Используйте `/summary` для просмотра итогов дня в любое время

### Настройка персональных уведомлений

Каждый пользователь может настроить уведомления под себя:

```
/settings - Посмотреть текущие настройки
/remind on - Включить уведомления
/remind off - Отключить уведомления
/remind time 21:30 - Установить время (например, 21:30)
/remind tz Europe/Moscow - Установить часовой пояс
```

**Примеры часовых поясов:**
- `Europe/Moscow` - Москва (МСК)
- `Europe/Kiev` - Киев
- `Asia/Almaty` - Алматы
- `America/New_York` - Нью-Йорк
- `UTC` - Всемирное время

Полный список часовых поясов: [List of tz database time zones](https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)

### Отключение бота

Если вы не хотите использовать Telegram бота:
- Не устанавливайте `TELEGRAM_BOT_TOKEN` в `.env`
- Или установите `BOT_REMINDERS_ENABLED=false` для отключения только напоминаний

Приложение будет работать без бота как обычное веб-приложение.

---

## Модель данных

### User (Пользователь)
- `id` — уникальный идентификатор
- `telegram_id` — ID пользователя в Telegram

### Project (Проект)
- `id` — уникальный идентификатор
- `name` — название проекта
- `short_name` — короткое имя (до 16 символов)
- `description` — описание проекта
- `goals` — цели проекта
- `creator_id` — связь с пользователем

### Task (Задача)
- `id` — уникальный идентификатор
- `title` — название задачи
- `status` — статус (TODO, IN_PROGRESS, DONE)
- `project_id` — связь с проектом

### Note (Заметка)
- `id` — уникальный идентификатор
- `content` — содержимое заметки
- `project_id` — связь с проектом

---

## Разработка

### Локальное тестирование (с Mock)

По умолчанию приложение работает с Telegram Mock - эмулятором API для локальной разработки:

```bash
# Запуск с mock (по умолчанию)
python run.py
```

В браузере вы увидите индикатор "🔧 Mock Mode" и приложение будет работать без подключения к Telegram.

### Тестирование через Telegram (без Mock)

Для тестирования в реальном Telegram Mini App:

```bash
# Отключить mock
TELEGRAM_MOCK=false python run.py

# В другом терминале запустите ngrok
ngrok http 5000
```

Подробная документация: [TELEGRAM_MOCK_README.md](TELEGRAM_MOCK_README.md)

### Быстрая настройка mock

| Команда | Описание |
|---------|----------|
| `python run.py` | С mock (локально, без интернета) |
| `TELEGRAM_MOCK=false python run.py` | Без mock (для Telegram) |
| Файл `.env`: `TELEGRAM_MOCK=false` | Отключить mock постоянно |

> **⚠️ Production**: В production обязательно установите `TELEGRAM_MOCK=false` для безопасности!

---